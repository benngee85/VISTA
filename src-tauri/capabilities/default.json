{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the primary VISTA window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
